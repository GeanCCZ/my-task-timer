import { accountUsersDomain } from './account-users-domain';

describe('accountUsersDomain', () => {
  it('should work', () => {
    expect(accountUsersDomain()).toEqual('account-users-domain');
  });
});
