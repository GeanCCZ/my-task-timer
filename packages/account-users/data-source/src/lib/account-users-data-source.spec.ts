import { accountUsersDataSource } from './account-users-data-source';

describe('accountUsersDataSource', () => {
  it('should work', () => {
    expect(accountUsersDataSource()).toEqual('account-users-data-source');
  });
});
