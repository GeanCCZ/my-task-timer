import { timeLogDataSource } from './time-log-data-source';

describe('timeLogDataSource', () => {
  it('should work', () => {
    expect(timeLogDataSource()).toEqual('time-log-data-source');
  });
});
